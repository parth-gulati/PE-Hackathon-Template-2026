global:
  resolve_timeout: 30s

route:
  receiver: 'discord'
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: 'discord'
    webhook_configs:
      - url: 'http://discord-webhook:9094'
        send_resolved: true
